<IfModule mod_rewrite.c>
    RewriteEngine On
    RewriteBase /quizisistem/  # Ganti dengan path yang sesuai jika perlu
    RewriteCond %{REQUEST_FILENAME} !-f
    RewriteCond %{REQUEST_FILENAME} !-d
    RewriteRule ^(.*)$ index.php/$1 [L]
</IfModule>